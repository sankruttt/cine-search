# CineSearch

A React-based movie search application using the OMDB API.

## Features

- 🔍 Search for movies by title
- 🎬 Display movie posters, ratings, and plots
- 📄 Pagination for search results
- 🔎 View detailed information in a modal

## Setup

1. Get your free API key from [OMDB API](http://www.omdbapi.com/apikey.aspx)

2. Replace `YOUR_OMDB_API_KEY` in `src/services/api.js` with your actual API key

3. Install dependencies:
   ```bash
   npm install
   ```

4. Start the development server:
   ```bash
   npm start
   ```

5. Open [http://localhost:3000](http://localhost:3000) in your browser

## Technologies Used

- React 18
- Axios for API calls
- OMDB API
- CSS3 with responsive design
